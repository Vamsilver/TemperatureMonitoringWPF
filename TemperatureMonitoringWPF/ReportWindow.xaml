<Window x:Class="TemperatureMonitoringWPF.ReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TemperatureMonitoringWPF"
        mc:Ignorable="d"
        Title="Report" Height="450" Width="800">
    <Grid Margin="10">
        <ListBox x:Name="Report" HorizontalAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="100" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding TitleDate}" />
                        <TextBlock Text="{Binding TitleFact}" />
                        <TextBlock Text="{Binding TitleStandart}" />
                        <TextBlock Text="{Binding TitleDeviation}" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <TextBox x:Name="Buffer" Foreground="Transparent" Background="Transparent"/>
    </Grid>
    
    
</Window>
